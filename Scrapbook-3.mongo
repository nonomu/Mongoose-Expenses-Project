db.expenses.aggregate([
                     { $match: { group: "fat" } },
                     { $group: { _id: "$group", total: { $sum: "$amount" } } },
                     { $sort: { total: -1 } }
                   ])